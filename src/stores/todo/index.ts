import { Todo } from "./type";
import {
    todoState,
    getFiltered,
    getCompleted,
    getIsAllCompleted,
} from "./store";

export { todoState, getFiltered, getCompleted, getIsAllCompleted };
export type { Todo };
