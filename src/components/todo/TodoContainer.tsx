import { TodoList, TodoNavigation } from ".";

function TodoContainer() {
    return (
        <section>
            <TodoList />
            <TodoNavigation />
        </section>
    );
}

export { TodoContainer };
